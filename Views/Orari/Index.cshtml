@model IEnumerable<WaveTheCave.Models.Orari>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrariGrotte)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrariGrotte)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdOrari }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdOrari }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdOrari })
        </td>
    </tr>
}

</table>
